import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SSV — Sistema de Supervisão de Viaturas",
  description: "Supervisão de viaturas, checklists, movimentações e manutenção.",
  other: {
    "codex-preview": "development",
  },
  icons: {
    icon: "/ssv-logo-transparent.png",
    shortcut: "/ssv-logo-transparent.png",
    apple: "/ssv-logo-transparent.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
